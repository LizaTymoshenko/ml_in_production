apiVersion: v1
kind: Service
metadata:
  name: test-fastapi-service
spec:
  selector:
    app: test-fastapi
  ports:
    - protocol: TCP
      port: 2000
      nodePort: 30001
      targetPort: 80
  type: NodePort
